# Infobric pricing page — audit (generated)

**Generated** by `application/scripts/make_page_tasks.py` from
`application/tasks/web_infobric-page-audit/` + `pages/pricing.md`.
Do not hand-edit: change the master task and re-run the script.

This variant exists so the page is launchable from the Playground UI, which
cannot pass harbor's `extra_instruction_paths`. For CLI runs, prefer the
master task with `application/scripts/make_page_jobs.py`.
